name: Update External Scripts

on:
  schedule:
    - cron: '* * * * *'
  workflow_dispatch:

jobs:
  parse_external_scripts:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout this repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: pip install requests

    - name: Parse External Scripts
      env:
        SCRIPT_URL_1: ${{ secrets.SCRIPT_URL_1 }}
        SCRIPT_URL_2: ${{ secrets.SCRIPT_URL_2 }}
      run: python scripts/parse_external_scripts.py

    - name: Commit and push changes
      if: steps.parse_external_scripts.outputs.changes_detected == 'true'
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
         git fetch origin
        git checkout ${{ github.ref_name }}
        git pull origin ${{ github.ref_name }}
        git add .
        git commit -m "Update external scripts"
        git push origin HEAD:${{ github.ref_name }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
